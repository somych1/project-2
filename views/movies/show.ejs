<header class="text-center">
	<h1><%= movie.sortableName %></h1>
</header>
<section class="container block">
	<div class="d-flex film-container">
		<div>
			<img src="<%= movie.media.posterStandard %>">
		</div>
		<div>
			<h6>Starring Actors: <%= movie.starringActors %></h6><br>
			<h6>Directors: <%= movie.directors %></h6><br>
			<h6>Genre: <%= movie.genre %></h6><br>
			<h6>Run time: <%= movie.runTime %></h6><br>
			<h6>Release Date: <%= movie.releaseDateUtc %></h6>
		</div>
		<div>
			<% if(button) { %>
				<form action="/movies?movieId=<%= movie.id%>&name=<%=movie.name%>&poster=<%= movie.media.posterStandard%>&link=<%= movie.links.self.href%>&mpaaRating=<%= movie.mpaaRating %>&runTime=<%= movie.runTime %>&synopsis=<%= movie.synopsis %>" method="POST">
					<button>ADD</button>
				</form>
			<% } else { %>
				<p>This movie is on your wishlist.</p>
			<% } %>
		</div>
	</div>
	<div>
		<h6><%= movie.synopsis %></h6>
	</div>
	<div>
		<video width="100%" src="<%= movie.media.trailerHd %>" controls>
		</video>
		<script type="text/javascript" src="/js/movieApp.js"></script>
	</div>

</section>
<footer class="block">
	
</footer>
